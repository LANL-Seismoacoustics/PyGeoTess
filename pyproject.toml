[build-system]
requires = ["setuptools", "cython", "numpy"]
build-backend = "setuptools.build_meta"

[project]
name = "pygeotess"
version = "0.2.2"

# [tool.setuptools]
# ext-modules = [
#   {name = "example", sources = ["example.pyx"]}
# ]
